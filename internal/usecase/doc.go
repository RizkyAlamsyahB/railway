// Package usecase implements the application's business logic.
//
// Each usecase orchestrates the flow of data between the domain layer and
// the repository layer. Usecases implement the interfaces defined in the
// domain package and are injected into delivery handlers via constructor
// injection.
package usecase
